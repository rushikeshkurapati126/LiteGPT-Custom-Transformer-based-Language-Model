# LiteGPT-Custom-Transformer-based-Language-Model
A lightweight implementation of a transformer-based language model built from scratch. Includes embeddings, attention mechanisms, and training on text data to generate meaningful responses through a simple chatbot interface.
